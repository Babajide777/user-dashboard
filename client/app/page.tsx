"use client";

import { Typography, Box, Container, Button, Skeleton } from "@mui/material";
import Image from "next/image";

export default function Home() {
  return (
    <Box
      sx={{
        backgroundColor: "#F6F6F6",
        width: "100%",
        maxWidth: "100%",
        paddingTop: "2rem",
      }}
    >
      <Typography>Jide</Typography>
    </Box>
  );
}
